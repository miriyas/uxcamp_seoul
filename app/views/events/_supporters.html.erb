<div id="section_supporters" class="section">
	<div class="centering">
		<h3>Sponsors</h3>
		<% if @supporters.blank? %>
			<p class="w100p tac">스폰서가 한군데도 없습니다.</p>
		<% else %>
			<ul class="supporters">
				<% @supporters.each do |supporter| %>
					<li class="supporter<%= " right_side" if @supporters.index(supporter) % 3 == 2 %>">
						<% if supporter.link.present? %>
							<%= link_to supporter.link, target: "_blank" do %>
								<div class="photo"><%= image_tag(supporter.photo.url(:camp5th), alt: supporter.name) if supporter.photo.present? %></div>
							<% end %>
						<% else %>
							<div class="photo"><%= image_tag(supporter.photo.url(:camp5th), alt: supporter.name) if supporter.photo.present? %></div>
						<% end %>
					</li>
				<% end %>
			</ul>
		<% end %>
		<p class="supporter_notice">UXCampSeoul은 많은 분들의 다양한 도움과 지원을 통해 진행되고 있습니다.<br>스폰서십 관련 연락은 <a href="mailto:uxcs@uxcamp.co.kr" class="tdu">uxcs@uxcamp.co.kr</a> 으로 메일 보내주시면 빠른 시일내에 답변드리겠습니다.</p>
	</div>
</div>
<div class="mobile_spacer"></div>