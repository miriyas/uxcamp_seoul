<ul class="breadcrumb">
  <li><%= link_to "행사 목록", admin_events_path %></li>
  <li class="active"><%= @event.title %></li>
</ul>

<% if notice.present? %>
	<p class="alert alert-success"><%= notice %></p>
<% end %>

<div class="event_show">
	<%= render 'information' %>
	<hr>
	<%= render 'program' %>
	<hr>
	<%= render 'organizer' %>
</div>